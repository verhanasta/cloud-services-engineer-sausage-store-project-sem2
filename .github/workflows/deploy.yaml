name: Sausage Store Deploy

on:
  push:
    branches:
      - main

env:
  NEXUS_HELM_REPO: ${{ secrets.NEXUS_HELM_REPO }}
  NEXUS_HELM_REPO_USER: ${{ secrets.NEXUS_HELM_REPO_USER }}
  NEXUS_HELM_REPO_PASSWORD: ${{ secrets.NEXUS_HELM_REPO_PASSWORD }}
  SAUSAGE_STORE_NAMESPACE: r-devops-magistracy-project-2sem-1367284807
  KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}

jobs:
  # Сборка и пуш образов в докерхаб:
  build_and_push_to_docker_hub:
    name: Push Docker image to DockerHub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        # Получение исходного кода из репозитория:
        uses: actions/checkout@v3
      - name: Set up Docker Buildx
        # Установка сборщика контейнеров Docker:
        uses: docker/setup-buildx-action@v2
      - name: Login to Docker
        # Авторизация на Docker Hub:
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Push Backend to DockerHub
        uses: docker/build-push-action@v4
        with:
          context: ./backend/
          push: true
          # Используем тег latest
          tags: verhanasta/sausage-backend:latest
          build-args: |
            VERSION=${{ github.sha }}
      - name: Push Frontend to DockerHub
        uses: docker/build-push-action@v4
        with:
          context: ./frontend/
          push: true
          # Используем тег latest
          tags: verhanasta/sausage-frontend:latest
      - name: Push Backend-report to DockerHub
        uses: docker/build-push-action@v4
        with:
          context: ./backend-report/
          push: true
          # Используем тег latest
          tags: verhanasta/sausage-backend-report:latest

  # Упаковываем helm-чарт и пушим его в Nexus
  add_helm_chart_to_nexus:
    name: Add Helm Chart To Nexus
    runs-on: ubuntu-latest
    needs: build_and_push_to_docker_hub

    # Передаем SHA коммита как output job
    outputs:
      chart_version: ${{ steps.set_version.outputs.CHART_SHA }}

    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Install Helm
        uses: azure/setup-helm@v1
        with:
          version: v3.14.2

      - name: Install helm-push plugin
        run: |
          helm plugin uninstall push || true
          helm plugin install https://github.com/chartmuseum/helm-push.git

      - name: Configure Helm Repository
        run: |
          helm repo add chartmuseum "${{ env.NEXUS_HELM_REPO }}" \
            --username="${{ env.NEXUS_HELM_REPO_USER }}" \
            --password="${{ env.NEXUS_HELM_REPO_PASSWORD }}"
          helm repo update

      - name: Set Chart Version to Commit SHA
        id: set_version # Используем этот ID для передачи output
        run: |
          # Устанавливаем версию чарта равной уникальному SHA коммита
          CHART_SHA="${{ github.sha }}"
          echo "Setting chart version to commit SHA: $CHART_SHA"
          
          # Обновляем версию в Chart.yaml с помощью yq
          yq e -i ".version = \"$CHART_SHA\"" ./sausage-store-chart/Chart.yaml
          
          # Устанавливаем SHA как output шага
          echo "CHART_SHA=$CHART_SHA" >> "$GITHUB_OUTPUT" 

      - name: Push Helm Chart
        run: |
          helm dependency update ./sausage-store-chart
          mkdir -p ./packaged-chart
          # Упаковываем чарт, который теперь имеет версию = SHA
          helm package ./sausage-store-chart -d ./packaged-chart
          helm cm-push ./packaged-chart/sausage-store-*.tgz "${{ env.NEXUS_HELM_REPO }}" \
            --context-path="/repository/anasteyshaverh-sausage-store" \
            --username="${{ env.NEXUS_HELM_REPO_USER }}" \
            --password="${{ env.NEXUS_HELM_REPO_PASSWORD }}"

  # Деплоим приложения в кластер Kubernetes с помощью helm-чарт
  deploy_helm_chart_to_kubernetes:
    name: Deploy Helm Chart To Kubernetes
    runs-on: ubuntu-latest
    needs: add_helm_chart_to_nexus

    steps:
      - name: Install Helm
        uses: azure/setup-helm@v1
        with:
          version: v3.14.2

      - name: Configure Kubernetes Access
        env:
          KUBECONFIG_BASE64: ${{ secrets.KUBE_CONFIG }}
        run: |
          mkdir -p ~/.kube
          echo "$KUBECONFIG_BASE64" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Add Helm Repository
        run: |
          helm repo add chartmuseum "${{ env.NEXUS_HELM_REPO }}" \
            --username "${{ env.NEXUS_HELM_REPO_USER }}" \
            --password "${{ env.NEXUS_HELM_REPO_PASSWORD }}"
          helm repo update

      - name: Deploy to Cluster.
        run: |
          # Используем SHA коммита, переданный как версия чарта
          CHART_VERSION="${{ needs.add_helm_chart_to_nexus.outputs.chart_version }}"
          echo "Deploying chart version $CHART_VERSION"
          helm upgrade --install sausage-store \
            chartmuseum/sausage-store \
            --version "$CHART_VERSION" \
            --namespace ${{ env.SAUSAGE_STORE_NAMESPACE }} \
            --set-string backend.image_tag="latest" \
            --set-string frontend.image_tag="latest" \
            --set-string backend-report.image_tag="latest"